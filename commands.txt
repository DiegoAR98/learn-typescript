Compile the TypeScript code to JavaScript:

tsc index.ts

This will generate an index.js file based on your TypeScript.

Run the generated JavaScript file with Node.js:


node index.js

npm install @faker-js/faker@7.6.0

npx parcel index.html

npm install @types/faker


directiores on the ts config from source and destination 

"rootDir"
and 
"outDir"

tsc -w (watcher for compiles)